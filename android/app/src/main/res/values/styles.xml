<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- Keep window background parchment so there is no black flash during the
             postSplashScreenTheme transition and before the WebView first frame renders.
             The WebView covers this entirely during normal app usage. -->
        <item name="android:windowBackground">@color/splashBackground</item>
    </style>

    <!-- Launch/splash theme — applied to MainActivity during startup.
         Parent Theme.SplashScreen enables the Android 12+ SplashScreen API.

         Pre-API 31 (Android < 12):
           - android:windowBackground shows immediately when the activity window opens.
             Set to dark color so there is no white flash before Capacitor's splash.
           - Capacitor SplashScreen plugin draws launch_screen.xml on top.

         API 31+ (Android 12+):
           - windowSplashScreenBackground: color behind the splash icon.
           - windowSplashScreenAnimatedIcon: icon shown in the center circle (TafsirKurd logo).
           - postSplashScreenTheme: theme applied to the activity after the system splash exits.
             Without this the activity inherits the SplashScreen theme and loses toolbar/status bar.

         The old generic @drawable/splash (blue-X Capacitor placeholder) is no longer referenced
         here — Capacitor now reads androidSplashResourceName = "launch_screen" from
         capacitor.config.ts and draws that instead. -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">

        <!-- Pre-API 31: parchment window shown immediately before Capacitor splash -->
        <item name="android:windowBackground">@color/splashBackground</item>

        <!-- API 31+ SplashScreen API: icon + background -->
        <item name="windowSplashScreenBackground">@color/splashBackground</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <!-- Match icon circle background to splash background so no badge/shape appears -->
        <item name="windowSplashScreenIconBackgroundColor">@color/splashBackground</item>

        <!-- Restore normal app theme after the system splash exits -->
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>

    </style>

</resources>
