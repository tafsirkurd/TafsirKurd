(async()=>{const r=await adminTranslationsAPI('bulk_update_by_key',{items:[{key_id:'dl.section',fields:{context:'Section title for the Downloads management area in user profile'}},{key_id:'dl.storage_used',fields:{context:'Label showing total storage used by all downloaded audio files'}},{key_id:'dl.no_reciters',fields:{context:'Empty state shown when no reciters have been downloaded yet'}},{key_id:'dl.full_quran',fields:{context:'Badge shown when the full Quran is downloaded for a reciter'}},{key_id:'dl.surahs',fields:{context:'Unit label — number of surahs downloaded (e.g. "12 surahs")'}},{key_id:'dl.manage',fields:{context:'Tooltip on the manage button for a downloaded reciter'}},{key_id:'dl.remove',fields:{context:'Tooltip on the delete button to remove a downloaded reciter'}},{key_id:'dl.not_downloaded',fields:{context:'Status label shown when nothing is downloaded for a reciter'}},{key_id:'dl.measuring',fields:{context:'Status shown while calculating the size of downloaded files'}},{key_id:'dl.downloading',fields:{context:'Status label shown while a download is actively in progress'}},{key_id:'dl.fully_downloaded',fields:{context:'Status label shown when the full Quran is downloaded for a reciter'}},{key_id:'dl.surah_downloaded',fields:{context:'Status label shown when a single surah is downloaded'}},{key_id:'dl.partial',fields:{context:'Status label shown when only some surahs are downloaded'}},{key_id:'dl.partial_surahs',fields:{context:'Partial download status — {n} is replaced with the number of downloaded surahs'}},{key_id:'dl.partial_issues',fields:{context:'Status shown when some files are downloaded but have integrity problems'}},{key_id:'dl.left',fields:{context:'Unit label for remaining download size (e.g. "12 MB left")'}},{key_id:'dl.failed_check',fields:{context:'Status shown when integrity verification fails for downloaded files'}},{key_id:'dl.wifi_only',fields:{context:'Toggle label for Wi-Fi only download mode in settings'}},{key_id:'dl.wifi_blocked',fields:{context:'Error shown when user tries to download on mobile data with Wi-Fi-only mode on'}},{key_id:'dl.verify',fields:{context:'Button label to trigger an integrity check on downloaded files'}},{key_id:'dl.verifying',fields:{context:'Status shown while the integrity check is running'}},{key_id:'dl.last_checked',fields:{context:'Label prefix for showing when integrity was last verified'}},{key_id:'dl.not_verified',fields:{context:'Status shown when integrity has never been checked'}},{key_id:'dl.repair',fields:{context:'Button label to re-download corrupted or incomplete files'}},{key_id:'dl.surah_word',fields:{context:'Singular unit label for one surah'}},{key_id:'dl.download_btn',fields:{context:'Primary button to start downloading a reciter'}},{key_id:'dl.continue_btn',fields:{context:'Button to resume a partially completed download'}},{key_id:'dl.redownload_btn',fields:{context:'Button to re-download a reciter that is already fully downloaded'}},{key_id:'dl.cancel',fields:{context:'Button to cancel an in-progress download'}},{key_id:'dl.tip_downloading',fields:{context:'Tooltip on reciter card download icon while a download is in progress'}},{key_id:'dl.tip_downloaded',fields:{context:'Tooltip on reciter card when fully downloaded — tells user to tap to manage'}},{key_id:'dl.tip_downloaded_s',fields:{context:'Short tooltip on reciter card when fully downloaded'}},{key_id:'dl.tip_partial',fields:{context:'Tooltip on reciter card when partially downloaded — tells user to tap to manage'}},{key_id:'dl.tip_partial_s',fields:{context:'Short tooltip on reciter card when partially downloaded'}},{key_id:'dl.tip_corrupt',fields:{context:'Tooltip on reciter card when files have integrity issues — tells user to tap to repair'}},{key_id:'dl.tip_corrupt_s',fields:{context:'Short tooltip on reciter card when files have integrity issues'}},{key_id:'dl.tip_offline',fields:{context:'Tooltip on reciter card download icon when nothing is downloaded yet'}},{key_id:'toast.dl_complete',fields:{context:'Toast notification shown when a download finishes successfully'}},{key_id:'toast.dl_removed',fields:{context:'Toast notification shown when downloaded files are deleted by the user'}},{key_id:'toast.dl_stopped',fields:{context:'Toast notification shown when a download is cancelled by the user'}}]});console.log('Result:',r);})().catch(e=>console.error('Error:',e));
